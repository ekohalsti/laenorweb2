DEBUG = False

SECRET_KEY = '{{ django_secret }}'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': '{{ db_name }}',
        'USER': '{{ db_user }}',
        'PASSWORD': '{{ db_pass }}',
        'HOST': '{{ db_host }}',
        'PORT': '5432',
    }
}
